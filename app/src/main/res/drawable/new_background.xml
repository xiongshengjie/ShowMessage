<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <corners
        android:radius="9dp"
        android:topLeftRadius="2dp"
        android:topRightRadius="2dp"
        android:bottomLeftRadius="2dp"
        android:bottomRightRadius="2dp"/><!-- 设置圆角半径 -->

    /*****

    通过不同角度，可以设置不同的形状 end
    *****/
    <!-- 渐变 -->
    <gradient
        android:startColor="@color/right_start"
        android:endColor="@color/right_stop"
        android:type="linear"
        android:angle="90"/>


    <!-- 间隔 -->
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp"/>

    <!-- 各方向的间隔 -->
    <!-- 大小 -->
    <size
        android:width="600dp"
        android:height="200dp"/>


</shape>